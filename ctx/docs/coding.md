# Coding

Use JavaScript and native ES modules. Prefer explicit constructor dependency injection, small replaceable components, provider-neutral contracts, structured inputs and outputs, deterministic tests, and no hidden global state.

Keep domain decisions separate from model adapters. A model response is untrusted external input: validate JSON shape, enums, and required fields before it can become a Semantic Interpretation Proposal. Do not add a framework merely to organise this reconnaissance.
